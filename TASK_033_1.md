Let's choose Option 3 (Auto-repair on import) which is the standard GIS architecture solution.

Please modify `backend/scripts/migrate_to_local.py`:
1. In `insert_batch` for the "fields" table insert statement, wrap the geometry value with `ST_MakeValid` to repair self-intersections on the fly:
   Change:
   `ST_SetSRID(ST_Force2D(ST_GeomFromEWKB(:geometry_ewkb)), 4326)`
   To:
   `ST_MakeValid(ST_SetSRID(ST_Force2D(ST_GeomFromEWKB(:geometry_ewkb)), 4326))`

2. In the `verify_geometry` and `verify_foreign_keys` validation steps (and anywhere else in verification queries), relax the geometry type check to allow both POLYGON and MULTIPOLYGON (since `ST_MakeValid` may split a self-intersecting polygon into a MULTIPOLYGON):
   Change:
   `OR GeometryType(geometry) <> 'POLYGON'`
   To:
   `OR GeometryType(geometry) NOT IN ('POLYGON', 'MULTIPOLYGON')`