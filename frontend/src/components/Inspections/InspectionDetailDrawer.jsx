import { useEffect,useRef,useState } from 'react';import { getFieldInspection } from '../../api/fieldInspections';import InspectionCard from './InspectionCard';import { formatDate,safeArray,safeString } from './inspectionPresentation';
export default function InspectionDetailDrawer({id,user,onNavigate,onClose,onAction,refreshToken,onLoaded}){const [item,setItem]=useState(null),[state,setState]=useState('loading'),[error,setError]=useState('');const generation=useRef(0),mounted=useRef(false),ctrl=useRef();useEffect(()=>{mounted.current=true;return()=>{mounted.current=false;generation.current++;ctrl.current?.abort()}},[]);useEffect(()=>{ctrl.current?.abort();ctrl.current=new AbortController();const current=++generation.current;setState('loading');getFieldInspection(id,ctrl.current.signal).then(value=>{if(!mounted.current||current!==generation.current||ctrl.current.signal.aborted)return;setItem(value);setState('ready');onLoaded?.(value)}).catch(err=>{if(!mounted.current||current!==generation.current||err?.code==='ERR_CANCELED')return;const status=err?.response?.status;setState(status===403?'403':status===404?'404':'error');setError('Не удалось загрузить осмотр.')});return()=>ctrl.current?.abort()},[id,refreshToken,onLoaded]);useEffect(()=>{const esc=e=>{if(e.key==='Escape')onClose()};document.addEventListener('keydown',esc);const old=document.body.style.overflow;document.body.style.overflow='hidden';return()=>{document.removeEventListener('keydown',esc);document.body.style.overflow=old}},[onClose]);return <div className="fixed inset-0 z-40 bg-black/30" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><aside className="ml-auto flex h-full w-full max-w-2xl flex-col bg-white shadow-xl" role="dialog" aria-modal="true" aria-label={`Осмотр #${id}`}><div className="flex items-center justify-between border-b border-agro-border p-4"><h2 className="font-bold">Осмотр #{id}</h2><button autoFocus onClick={onClose} className="rounded p-2 focus:ring-2 focus:ring-agro-accent" aria-label="Закрыть осмотр">✕</button></div><div className="flex-1 overflow-y-auto overflow-x-hidden p-4" aria-live="polite">{state==='loading'&&<p>Загружаем осмотр…</p>}{state==='403'&&<p role="alert">Недостаточно прав для просмотра осмотра.</p>}{state==='404'&&<p role="alert">Осмотр не найден или недоступен.</p>}{state==='error'&&<div role="alert"><p>{error}</p><button onClick={()=>onNavigate('field-inspection-detail',id)} className="btn-primary mt-3 px-3 py-2">Повторить</button></div>}{state==='ready'&&item&&<div className="space-y-4"><InspectionCard inspection={item} user={user} onNavigate={onNavigate} onAction={onAction} compact/><section className="card space-y-2 p-4 text-sm"><h3 className="font-semibold">Подробности</h3><p><b>Инструкции:</b> {safeString(item.instructions)}</p><p><b>Сводка источника:</b> {item.source_priority||'—'} · {Number.isFinite(item.source_attention_score)?item.source_attention_score:'—'} · {formatDate(item.source_observation_date)}</p><p><b>Коды причин:</b> {safeArray(item.source_reason_codes).filter(x=>typeof x==='string').join(', ')||'—'}</p><p><b>Итог осмотра:</b> {safeString(item.completion_summary)}</p><p><b>Причина отмены:</b> {safeString(item.cancellation_reason)}</p></section></div>}</div></aside></div>}
